services:
  panel:
    image: ghcr.io/spancerxing/api-quota-panel:latest
    # build: .          # uncomment for local rebuild
    ports:
      - "9876:9876"
    env_file:
      - .env
    restart: unless-stopped
